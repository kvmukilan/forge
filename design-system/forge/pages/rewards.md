# Rewards override

- Coin and gem balances form one compact two-column signal strip on phones.
- Reward destinations are navigable rows with icon, title, explanation, and directional affordance.
- Do not repeat an `Open` button inside a card that is already a link.
- Keep reward color semantic: amber for coins, cyan for rare gems, mint for navigation and earned progress.
